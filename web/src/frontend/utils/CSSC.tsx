import { css } from "@emotion/react";

export const baseFormCss = css({
    width: '100%',
    maxWidth: 600,
});

export const h100w100 = css({
    height: "100%",
    width: "100%"
});

export const pageWrapperCss = css({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'flex-start',
    padding: '2rem',
    height: "100%"
});